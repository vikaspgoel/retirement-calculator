---
description: React/JSX coding standards
globs: "**/*.tsx"
alwaysApply: false
---

# React/JSX Standards

## Apostrophes in JSX Text

Use regular apostrophes `'` in JSX text - the ESLint rule is disabled in this project.

```tsx
// ✅ GOOD - use regular apostrophes
<p>Let's go</p>
<p>It's working</p>
```

## Still Escape These Characters

| Character | Use Instead |
|-----------|-------------|
| `<` | `&lt;` |
| `>` | `&gt;` |
